{
  "name": "@vinary-tree/vinary-tree-property-tests",
  "version": "4.0.0-rc.1",
  "private": true,
  "type": "module",
  "description": "C8 fast-check property tests for the native runtime in an isolated private package",
  "scripts": {
    "test": "node --test native.property.test.mjs"
  },
  "devDependencies": {
    "fast-check": "^4.0.0"
  }
}
